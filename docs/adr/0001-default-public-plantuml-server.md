# Use the public PlantUML server by default

Fuxian renders PlantUML through the public server by default so a finished document works without setup. Users can replace it with a local or private server; the interface must disclose that PlantUML source is sent to the configured service rather than silently implying local rendering.
