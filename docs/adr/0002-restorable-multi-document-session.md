# Use a restorable multi-document session

Fuxian runs as a single application instance with one main window but keeps multiple independent Markdown documents open at once. It restores the active document, the ordered open-document set, and each document's reading position after restart or an unexpected exit; this provides continuity without introducing folders, projects, or a filesystem tree.
