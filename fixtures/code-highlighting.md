# 代码高亮主题

以下示例用于检查常用语言的语法高亮与主题切换。

## Python

```python
def greet(name: str) -> str:
    return f"Hello, {name}"
```

## JavaScript

```javascript
const loadDocument = async (path) => {
  const response = await fetch(path);
  return response.text();
};
```

## Java

```java
public final class DocumentReader {
  private final String title = "浮现";
}
```
